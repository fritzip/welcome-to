version: "3.8"

services:
  static:
    container_name: welcome
    image: nginx
    env_file: .env
    volumes:
      - ./src:/usr/share/nginx/html:ro
    restart: unless-stopped
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.routers.welcome.entrypoints=websecure"
      - "traefik.http.routers.welcome.rule=Host(`${DOMAINNAME}`) && PathPrefix(`/${PREFIX}`)"
      - "traefik.http.middlewares.welcome-stripprefix.stripprefix.prefixes=/${PREFIX}"
      - "traefik.http.routers.welcome.middlewares=trailing-slash@file,welcome-stripprefix@docker"

networks:
  proxy:
    external: true
